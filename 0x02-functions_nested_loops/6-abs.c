#include "main.h"
/**
 * _abs - computes the absoulute value of an integer
 *
 * @x: the number to be computed
 *
 * Return: absoulte value of an integer
 */
int _abs(int x)
{
	int y = -1;

	if (x < 0)
	{
		return (y * x);
	}
	return (x);
}
